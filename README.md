# Active-Directory-Home-Lab
Built a Windows Server Active Directory environment with domain-joined Windows clients.

I downloaded Oracle VirtualBox and setup Windows Server and Windows client VM's. In the setup I enabled a 2nd network adapter for an internal network so the server and clients could communicate.

![](images/image40.png)
![](images/image.png)
